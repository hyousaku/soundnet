[package]
name = "roc-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal hand-written FFI bindings for the roc-toolkit C library (libroc)."
links = "roc"

[lib]
name = "roc_sys"
path = "src/lib.rs"

[build-dependencies]
pkg-config = "0.3"
