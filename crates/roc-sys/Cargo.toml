[package]
name = "roc-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FFI bindings for the roc-toolkit C library (libroc), generated from its headers."
links = "roc"

[lib]
name = "roc_sys"
path = "src/lib.rs"

[build-dependencies]
bindgen = "0.72.1"
pkg-config = "0.3"
