# SoundNet 改善タスク引き継ぎ（2026-08-18 レビュー）

コードベース全体を読み直して見つけた課題を、優先度順に独立したタスクへ分割した
ものです。**1タスク＝1セッション＝1コミット**で進めてください。まとめて渡すと
互いに無関係な変更が1つの巨大な diff になって検証できなくなります。

各項目には検証状態を明記しています:

- **[確認済み]** — コードの該当行を読んで裏を取った事実
- **[推定]** — 理屈は通っているが実機・実測での確認はまだ

推奨実行順: H1 → H2 → H4 → H5 → H6 → H9 → H7 → H8 → H3
（バグ → 運用上の正しさ → 掃除 → フォーマット → CI → 大物の順。
H3 だけは設計判断を含むので、着手前に人間の承認を取ること。）

## 進捗

| | 状態 | コミット |
|---|---|---|
| H1 try_start の競合 | 完了 | `f89249e` |
| H2 config.toml の非アトミック書き込み | 完了 | `93bd247` |
| H3 キャプチャデバイスの共有 | **未着手（要承認）** | — |
| H4 route_port の衝突 | 完了 | `35162da` |
| H5 ストール表示 | 完了 | `e1570a2` |
| H6 jitter 列 / state.stats | 完了 | `7b0ccb1`, `682deb1` |
| H7 S24_LE 対応 | 完了（合成テストのみ、実機デバイス無し） | `977b59c` |
| H8 snd-aloop 統合テスト | 完了（CI 不可と判明、手元実行に変更） | `a0cab00` |
| H9 クリップ計測の位置 | 完了 | `7ce0806` |

H8 の結論: GitHub の `ubuntu-24.04` ランナーは `snd-aloop` を読み込めない。
「読み込まれていない」ではなく `/lib/modules/$(uname -r)` ツリー自体が存在せず、
`/proc/asound` も無い（CI 上で実測）。テストは `#[ignore]` 付きで
`crates/soundnet-engine/tests/loopback.rs` に置き、README に実行手順を書いた。
CI はコンパイルだけはするので腐らない。

---

## 共通の前提（各タスクの冒頭に貼る）

```
このリポジトリは SoundNet(Rust + roc-toolkit による LAN 低遅延オーディオ)です。
作業に入る前に必ず README.md、crates/soundnet-engine/src/pipeline/send.rs と
recv.rs、rt.rs のモジュールコメントを読んでください。そこに書かれている設計上の
不変条件は実機デバッグの結果であり、壊してはいけません。特に:

1. デバイスのクロックが唯一のクロック。ALSA と roc の間にバッファを置かない。
   roc は ROC_CLOCK_SOURCE_EXTERNAL のまま。
2. オーディオスレッドのループは SCHED_FIFO で走るため、1周回に必ずちょうど
   1つのブロッキング点を持つ。エラーパスを含め、ブロッキングせずに次の周回に
   入らない(現在のブロッキング点は snd_pcm_wait + タイムアウト)。
3. RT 権限が取れない環境でも警告を出して通常優先度で動き続ける。
4. 再生は start_threshold=buffer_size で満タンから始める(pcm.rs 参照)。
5. デバイスは番号でなくカード ID で命名する(devices.rs::alsa_device 参照)。

コード中の長い説明コメントは事故の記録であり資産です。削らず、変更箇所には
同じ水準で書き足すこと。

- 依頼された範囲だけを変更する。ついでのリファクタリングはしない。
- cargo fmt --all --check / cargo clippy --workspace --all-targets -- -D warnings /
  cargo test --workspace が通ることを確認する(CI もこの3つを走らせる)。
- web/ を触ったら cd web && npm run build も通すこと。
- 実機テストが必要な変更は、人間が何をどう確認すべきか具体的に提示する。
  このサンドボックスにサウンドカードは無い。
```

---

## H1 —【最優先・バグ・確認済み】try_start の競合でパイプラインがリークする

**背景**

`routing.rs::try_start` は同一ルートに対して3経路から並行に呼ばれうる:
UI/gossip 経由の `apply_route`、mDNS 発見時の `retry_pending_for_peer`、
3秒毎の `spawn_route_supervisor`。流れは

```
state.running.get(&route.id)   … 走っていないことを確認
try_start_inner(...).await     … ← await 点が複数ある
state.running.insert(...)      … 出来た RunningRoute を登録
```

で、get と insert の間に鍵がない。2つの呼び出しが両方 get を通過すると
両方がパイプラインを spawn し、**後の insert が先の RunningRoute を上書きする。**
上書きされた側は `stop_and_join` されずに drop される — `JoinHandle` の drop は
デタッチなので、**スレッドは stop フラグを立てる手段ごと失われ、デバイスを
掴んだまま永久に走り続ける。** hw: デバイスなら2本目の open が EBUSY で落ちて
「デバイスが開けない」という別の症状にもなる(どちらが勝つかは不定)。

**やること**

1. spawn の前に登録を済ませる形に変える。`DashMap::entry` で vacant のときだけ
   進む、または `state.starting: DashSet<RouteId>` のガードを取り、取れなければ
   即 return する。ガードは await を跨いで保持されるので、panic 時にも外れる
   よう Drop ガード(または明示的な全パス解放)にすること。
2. `apply_route` の「変更があったら stop→start」の並びが、このガードの下でも
   正しく直列化されることを確認する。
3. なぜ鍵が必要なのかを、呼び出し3経路を名指しするコメントで残す。

**やらないこと**

- パイプライン(send.rs/recv.rs)には触らない。これは routing の登録順の問題。
- グローバルな大きい Mutex で全ルートを直列化しない。ルート単位で。

**受け入れ条件**

- get→insert の間に同一ルートの二重 spawn が構造的に不可能になっている
  (レビューで示せること。並行の再現テストが書けるなら尚良いが、
  本番コードにテスト用フックを撒いてまでは要求しない)。
- 既存の routing テスト(5本)が通る。
- 実機確認手順: ルートを張った状態で `systemctl restart` を数回 →
  `ps -eLf | grep 'send-\|recv-'` でスレッド数がルート数と一致すること。

---

## H2 —【バグ・確認済み】config.toml の書き込みが非アトミック

**背景**

`config.rs::Config::save` は `std::fs::write` で直接上書きしている。
ルート変更のたびに `persist()` が全体を書き直すので、**書き込み中の停電・
クラッシュで config.toml が半端に切れ、全ルートと node_id を失う。**
node_id を失うと再起動後に別ノードとして現れる(過去に潰したゴースト
ノード問題の再来)。無人自動復帰を目標とするシステムでここが弱いのは矛盾。

**やること**

同一ディレクトリに一時ファイルを書き、`sync_all`(fsync) してから
`std::fs::rename` で置き換える。rename は同一ファイルシステム内なら
アトミック。~15行の変更。

**受け入れ条件**

- 書き込み関数を分離し、「途中で失敗しても旧ファイルが無傷」をテストで示す
  (一時ファイル書き込みに失敗するケースを注入できる形にする)。
- 既存の config round-trip テスト3本が通る。

---

## H3 —【大物・機能・要承認】キャプチャデバイスの共有(1入力 → N宛先)

**背景**

パイプラインはデバイスを排他所有するため、同じ入力を2つの宛先へ送れない
(2本目のルートが EBUSY で永久 retrying)。実運用の要望として
「PC1 の I/F の IN1-2 を PC2 と PC3 の両方へ」が既に挙がっている。

**やること(設計から)**

1. まず設計を書いて人間の承認を得る。勝手に実装まで進まない。
2. 方向性の候補: デバイス毎に1つの「オーナー」キャプチャスレッドを立て、
   そこから各ルートの roc sender へファンアウトする。不変条件との整合が核心:
   - オーナーだけが ALSA でブロックする(1ループ1ブロッキング点はオーナーが担う)
   - 各ルートの送信は `roc_sender_write`(EXTERNAL、非ブロッキング)だけなら、
     オーナースレッドが全 sender に直接書く形にでき、中間バッファを増やさずに
     済む可能性がある。リングを再導入するなら send.rs のモジュールコメントに
     書かれた「リングを消した理由」と正面から向き合うこと。
3. スコープはキャプチャ共有のみ。**再生側の共有(ミキシング)はやらない** —
   ミキサーが必要になり別の大きさの仕事になる。

**受け入れ条件**

- 同一入力デバイスから2宛先へのルートが同時に鳴る。
- 既存の1対1ルートの遅延・xrun が悪化しない(実機で前後比較)。
- チャンネル窓(offset)がルート毎に独立して機能する。

---

## H4 —【正しさ・確認済み】route_port の衝突確率はコメントの記述より遥かに高い

**背景**

`routing.rs::route_port` はルート id のハッシュを 1000 バケットに畳む。
コメントは「1000 ルートまで衝突しにくい」と書いているが、これは誕生日問題で、
実際は **10本で約4%、32本で約39%** の確率で2ルートが同じポルトの三つ組に
落ちる。衝突時は後から起動した側の `roc_receiver_bind` が EADDRINUSE で失敗し
永久 retrying になる(音が混ざることはない。reuse_address=0 を確認済み)。
症状は「なぜか1本だけ起動しないルート」で、原因がエラーからは読めない。

**やること**

1. bind 失敗時、自エンジンの全ルートのポートを計算して衝突相手を特定し、
   「ルート X とポートが衝突している。どちらかを作り直せば別ポートに載る」
   というエラーメッセージにする(受信側エンジンは自分宛ルートを全部知って
   いるので、これはローカルで計算できる)。
2. コメントの確率の記述を誕生日問題の正しい数字に訂正する。
3. 決定論的な再割当(衝突時に次のバケットへ)は**やらない** — 両端のエンジンが
   交渉なしに同じ答えに辿り着く必要があり、ルート集合が一時的に食い違うと
   別ポートを聴き合う。エラーの可視化だけで実用上は足りる。

**受け入れ条件**

- 衝突を人工的に作るテスト(同じバケットに落ちる id の組を見つけて固定)で、
  エラーメッセージが相手ルートを名指しすること。
- `route_ports_never_partially_overlap` テストが引き続き通る。

---

## H5 —【可観測性・確認済み】ストールしたルートが UI で "ok" のまま

**背景**

デバイスが応答しなくなると `snd_pcm_wait` のタイムアウトが続き、ログには
`device stalled?` が出る(pipeline/mod.rs::STALL_WARN_AFTER)。しかしスレッドは
生きているので `is_dead()` は false、stats pump は `RouteHealth::Ok` を報告し、
**UI では無音のルートが緑の "ok" のまま。** ログを読まない限り分からない。

**やること**

1. SendHandle / RecvHandle に「最後に進捗した時刻」または stalled フラグの
   Atomic を追加し、ループのタイムアウト経路で更新する。
2. stats pump がそれを読み、`RouteHealth` に Stalled 相当を追加して報告する
   (protocol 変更 → web/src/protocol.ts と RouteEditor.tsx の health 列も)。
3. 表示は「stalled — device not producing/accepting audio」のように、
   デバイス側の問題であることが読める文言にする。

**受け入れ条件**

- プロトコル変更なので両端更新が必要な旨をコミットメッセージに明記。
- 実機確認手順: ルート再生中に USB を引き抜き、UI の health が数秒で
  stalled 表示に変わり、挿し直すと ok に戻ること。

---

## H6 —【掃除・確認済み】死んでいる jitter 列と state.stats フィールド

**背景**

1. `jitter_ns` は 0 で初期化されて以降誰も書かない(libroc 0.4 が niq_latency を
   落としたため)。UI の jitter 列は**常に 0.00 ms** を表示していて、
   「測っていない」が「ジッタゼロ」に見える — このプロジェクトが何度も潰して
   きた「正直でない表示」がまだ1つ残っている。
2. `EngineState::stats` は `remove_route` の remove 以外誰も触らない
   (routing.rs の T6 テスト時に発見済み、テストコメントにも記載)。

**やること**

- jitter: 列ごと消すか、`query_e2e_ns` の履歴から標準偏差を計算して実測にする。
  消す方が誠実で小さい。実測にするなら「何の分散か」を列ツールチップで定義
  すること。
- `stats` フィールドとそれに触る全箇所(remove、テストの sentinel)を削除する。

**受け入れ条件**

- 2つは別コミットに分ける。
- jitter を消す場合、protocol から `jitter_ms` を落とすので両端更新の明記。

---

## H7 —【フォーマット・据え置き済み】S24_LE(4バイト右詰め)対応

**背景**

ALSA の 24bit 形式は3つあるが、SoundNet は S24_3LE と S32_LE しか試さない。
**S24_LE しか出せないデバイスは「24bit 非対応」に見え、フォーマット代替にも
現れない**(format.rs の FALLBACK_FORMATS のコメントに生きた実例として記載済み、
修正2として意図的に据え置いた経緯がある)。

**やること**

1. `SampleFormat` に S24Le4(仮名)を追加。**enum に variant を足すと
   format.rs の網羅ガード(`all_formats` の `_` 無し match)がコンパイルを
   止める**ので、そこから誘導される全箇所(変換、CANDIDATE_FORMATS、
   FALLBACK_FORMATS、UI の FORMATS)を埋める。
2. 変換: 下位24bit 有効・符号は bit23、上位8bit は符号拡張(読み)/ゼロか
   符号(書き)。既存の S24_3LE の round-trip テストと同型のテストを書く。
3. FALLBACK_FORMATS 内の優先順は「解像度を落とさない」原則で S24_3LE の直後。

**受け入れ条件**

- round-trip テストと、`s16_is_only_substituted_when_nothing_else_works` の
  網羅テストが新 variant 込みで通る。
- 実機確認: 該当デバイスを持っている場合のみ。無ければ合成テストで足りる旨を
  報告に明記する。

---

## H8 —【CI・推定】snd-aloop による実 ALSA ループバック統合テスト

**背景**

テスト54本は全て純粋関数かモック相当で、**実際に ALSA を通る自動テストは
ゼロ**。tools/measure-latency.py の ALSA パスも実機未検証のまま。GitHub の
ubuntu ランナーは `sudo modprobe snd-aloop` が通る可能性が高い(未検証)。

**やること**

1. まずランナーで `sudo modprobe snd-aloop` が通るか単体で確かめる。
2. 通るなら: エンジンを2プロセス起動 → REST でセルフループのルートを作成 →
   Loopback カード経由でトーンを流し、逆側 subdevice から読んで
   周波数とレベルを検証する統合テスト(`tests/` に、CI では専用ジョブ)。
3. 通らないなら: 同じものを手元実行用スクリプトとして整備し、README に
   実行手順を書く。**できなかった場合は、できなかったと報告する。**

**受け入れ条件**

- CI で走る場合: 既存ジョブと分離し、flaky なら再試行1回まで。テストを
  通すためにテスト自体を緩めない。
- ローカル整備で終わる場合: 実機での実行ログを人間から取得して確認する。

---

## H9 —【小粒・確認済み】クリップ計測がフェード適用後にある

**背景**

recv.rs はランプ(`fade.apply`)の**後**にクリップを数えるので、ランプ中は
ソースが振り切れていてもカウントされない。復帰直後こそゲイン事故を検出
したい場面で、まさにその間だけ計器が目を瞑る。`raw_peak` はフェード前に
取ってあるので、over のカウントだけ前へ移せばよい。

**やること**

- `over` のカウントをフェード適用前の `floats` に対して行う(レベルメーターは
  今のまま「実際に出る音」= フェード後で正しい。クリップは「ソースの状態」
  なのでフェード前が正しい — この非対称をコメントで明記)。

**受け入れ条件**

- 数行の変更。既存テストが通ること。

---

## 参考: 今回のレビューで「問題なし」を確認した箇所

- `PortRef.channel_offset` には `#[serde(default)]` があり、旧バージョンとの
  gossip 互換は保たれている。
- roc の `no_playback_timeout` 等の 0 は「既定値を使う」で正しい。
- 受信ポートの `reuse_address=0` により、ポート衝突が音の混入になることはない
  (H4 のとおり bind 失敗として現れる)。
- fade は armed 前とランプ消化後にビット完全パススルー(テストで固定済み)。
